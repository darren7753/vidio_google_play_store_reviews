name: Show Me the S3cr3tz
on:
  push

jobs:
  debug:
    name: Debug
    runs-on: ubuntu-latest

    steps:
    - name: Check out code
      uses: actions/checkout@v2

    - name: Print secret values
      env:
        DEBUG_PASSWORD: ${{ secrets.MONGODB_URL }}
      run: |
        echo "${{ secrets.MONGODB_URL }}" | sed 's/./& /g'